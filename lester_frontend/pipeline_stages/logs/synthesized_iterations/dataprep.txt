
 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import os
    import warnings
    from transformers import pipeline
    import lester as ld

    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']
    customer_data = {}

    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def matches_usecase(text):
        return "complaint" in text or "bank account" in text

    def sanitize(text):
        return text.lower()

    # Load customers data
    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'])

    # Filter customers by target countries
    customers_df = customers_df.filter("country in @target_countries")

    # Create is_premium column
    customers_df = customers_df.project('is_premium', ['level'], lambda level: level == 'premium')

    # Select relevant columns
    customers_df = customers_df[['customer_email', 'bank', 'country', 'is_premium']]

    # Load mails data
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'])

    # Create mail_year column
    mails_df = mails_df.project('mail_year', ['raw_date'], lambda raw_date: int(raw_date.split("-")[0]))

    # Filter mails from 2022 onwards
    mails_df = mails_df.filter("mail_year >= 2022")

    # Join mails with customers data
    merged_df = ld.join(mails_df, customers_df, left_on='email', right_on='customer_email')

    # Create title column
    merged_df = merged_df.project('title', ['mail_subject'], sanitize)

    # Create text column
    merged_df = merged_df.project('text', ['mail_text'], sanitize)

    # Create sentiment column
    merged_df = merged_df.project('sentiment', ['mail_text'], lambda mail_text: sentiment_predictor(mail_text)[0]['label'].lower())

    # Select final columns
    result_df = merged_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import os
    import warnings
    import lester as ld
    from transformers import pipeline

    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']
    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def sanitize(text):
        return text.lower()

    def is_premium(level):
        return level == 'premium'

    def extract_year(raw_date):
        return int(raw_date.split("-")[0])

    # Read customer data
    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=',')
    customers_df = customers_df.filter("country in @target_countries")
    customers_df = customers_df.project('is_premium', ['level'], is_premium)
    customers_df = customers_df[['customer_email', 'bank', 'country', 'is_premium']]

    # Read mails data
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=',')
    mails_df = mails_df.project('mail_year', ['raw_date'], extract_year)
    mails_df = mails_df.filter("mail_year >= 2022")

    # Join customer and mails data
    combined_df = ld.join(mails_df, customers_df, left_on='email', right_on='customer_email')

    # Process combined data
    combined_df = combined_df.project('title', ['mail_subject'], sanitize)
    combined_df = combined_df.project('text', ['mail_text'], sanitize)
    combined_df = combined_df.project('sentiment', ['mail_text'], lambda x: sentiment_predictor(x)[0]['label'].lower())

    # Select the required columns
    result_df = combined_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import lester as ld
    import os
    from transformers import pipeline
    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    import warnings

    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']
    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def matches_usecase(text):
        return "complaint" in text or "bank account" in text

    def sanitize(text):
        return text.lower()

    # Read customer data
    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=',')

    # Filter for target countries
    customers_df = customers_df.filter(f'country in {target_countries}')

    # Define is_premium
    def define_is_premium(level):
        return level == 'premium'
    
    customers_df = customers_df.project('is_premium', ['level'], define_is_premium)

    # Read mails data
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=',')

    # Filter for mails after 2021
    mails_df = mails_df.filter('int(raw_date.split("-")[0]) >= 2022')

    # Join customers and mails data
    merged_df = ld.join(mails_df, customers_df, left_on='email', right_on='customer_email')

    # Sanitize mail subject
    merged_df = merged_df.project('title', ['mail_subject'], lambda mail_subject: sanitize(mail_subject))

    # Sanitize mail text
    merged_df = merged_df.project('text', ['mail_text'], lambda mail_text: sanitize(mail_text))

    # Predict sentiment
    def predict_sentiment(mail_text):
        return sentiment_predictor(mail_text)[0]['label'].lower()
    
    merged_df = merged_df.project('sentiment', ['mail_text'], predict_sentiment)

    # Select final columns
    result_df = merged_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import lester as ld
    from transformers import pipeline

    target_countries = ['UK', 'DE', 'FR']
    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def sanitize(text):
        return text.lower()

    def get_is_premium(level):
        return level == 'premium'

    def get_mail_year(raw_date):
        return int(raw_date.split("-")[0])

    def predict_sentiment(mail_text):
        return sentiment_predictor(mail_text)[0]['label'].lower()

    # Load and process customer data
    customers = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=',')
    customers = customers.filter('country in @target_countries')
    customers = customers.project('is_premium', ['level'], get_is_premium)
    customers = customers[['customer_email', 'bank', 'country', 'is_premium']]

    # Load and process mail data
    mails = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=',')
    mails = mails.project('mail_year', ['raw_date'], get_mail_year)
    mails = mails.filter('mail_year >= 2022')

    # Join customer and mail data
    joined_df = ld.join(mails, customers, left_on='email', right_on='customer_email')

    # Process the joined data
    joined_df = joined_df.project('title', ['mail_subject'], sanitize)
    joined_df = joined_df.project('text', ['mail_text'], sanitize)
    joined_df = joined_df.project('sentiment', ['mail_text'], predict_sentiment)

    # Select final columns
    result_df = joined_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import os
    from transformers import pipeline
    import warnings
    import lester as ld

    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']

    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def matches_usecase(text):
        return "complaint" in text or "bank account" in text

    def sanitize(text):
        return text.lower()

    # Read and filter customer data
    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=',')
    customers_df = customers_df.filter('country in @target_countries')
    customers_df = customers_df.project('is_premium', ['level'], lambda level: level == 'premium')

    # Read mail data
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=',')

    # Filter mail data
    mails_df = mails_df.project('mail_year', ['raw_date'], lambda raw_date: int(raw_date.split("-")[0]))
    mails_df = mails_df.filter('mail_year >= 2022')

    # Join customer data with mail data
    merged_df = ld.join(mails_df, customers_df, left_on='email', right_on='customer_email')

    # Process data
    merged_df = merged_df.project('title', ['mail_subject'], lambda mail_subject: sanitize(mail_subject))
    merged_df = merged_df.project('text', ['mail_text'], lambda mail_text: sanitize(mail_text))
    merged_df = merged_df.project('sentiment', ['mail_text'], lambda mail_text: sentiment_predictor(mail_text)[0]['label'].lower())

    # Select relevant columns
    result_df = merged_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import lester as ld
    import os
    from transformers import pipeline
    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    import warnings

    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']
    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def sanitize(text):
        return text.lower()

    # Read customer data
    customers_df = ld.read_csv(customers_file, header=None, names=["customer_id", "customer_email", "bank", "country", "level"], sep=",")
    customers_df = customers_df.filter('country in @target_countries')
    customers_df = customers_df.project('is_premium', ['level'], lambda level: level == 'premium')
    customers_df = customers_df[['customer_email', 'bank', 'country', 'is_premium']]

    # Read mail data
    mails_df = ld.read_csv(mails_file, header=None, names=["mail_id", "email", "raw_date", "mail_subject", "mail_text"], sep=",")
    mails_df = mails_df.project('mail_year', ['raw_date'], lambda raw_date: int(raw_date.split("-")[0]))
    mails_df = mails_df.filter('mail_year >= 2022')

    # Join the dataframes
    joined_df = ld.join(mails_df, customers_df, left_on='email', right_on='customer_email')

    # Sanitize and predict sentiment
    joined_df = joined_df.project('title', ['mail_subject'], sanitize)
    joined_df = joined_df.project('text', ['mail_text'], sanitize)
    joined_df = joined_df.project('sentiment', ['mail_text'], lambda mail_text: sentiment_predictor(mail_text)[0]['label'].lower())

    # Select the final columns for output
    result_df = joined_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import os
    import lester as ld
    from transformers import pipeline
    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    import warnings

    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']
    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def sanitize(text):
        return text.lower()

    # Read and process customer data
    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=',')
    customers_df = customers_df.filter('country in @target_countries')
    customers_df = customers_df.project('is_premium', ['level'], lambda level: level == 'premium')
    customer_data = customers_df[['customer_email', 'bank', 'country', 'is_premium']]

    # Read and process mail data
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=',')
    mails_df = mails_df.project('mail_year', ['raw_date'], lambda raw_date: int(raw_date.split("-")[0]))
    mails_df = mails_df.filter('mail_year >= 2022')

    # Join customer and mail data
    joined_df = ld.join(mails_df, customer_data, left_on='email', right_on='customer_email')

    # Sanitize text and predict sentiment
    joined_df = joined_df.project('title', ['mail_subject'], sanitize)
    joined_df = joined_df.project('text', ['mail_text'], sanitize)
    joined_df = joined_df.project('sentiment', ['mail_text'], lambda mail_text: sentiment_predictor(mail_text)[0]['label'].lower())

    # Select and return the final dataframe
    result_df = joined_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]
    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import lester as ld
    import os
    from transformers import pipeline
    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    import warnings

    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']
    customer_data = {}

    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def sanitize(text):
        return text.lower()

    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=',', parse_dates=False)
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=',', parse_dates=False)

    # Filter customers based on target countries
    customers_df = customers_df.filter('country in @target_countries')

    # Create is_premium column
    customers_df = customers_df.project('is_premium', ['level'], lambda x: x == 'premium')

    # Select relevant columns
    customers_df = customers_df[['customer_email', 'bank', 'country', 'is_premium']]

    # Filter mails based on year
    mails_df = mails_df.project('mail_year', ['raw_date'], lambda x: int(x.split("-")[0]))
    mails_df = mails_df.filter('mail_year >= 2022')

    # Join customers and mails dataframes
    merged_df = ld.join(mails_df, customers_df, left_on='email', right_on='customer_email')

    # Create title and text columns with sanitized text
    merged_df = merged_df.project('title', ['mail_subject'], sanitize)
    merged_df = merged_df.project('text', ['mail_text'], sanitize)

    # Create sentiment column
    merged_df = merged_df.project('sentiment', ['mail_text'], lambda x: sentiment_predictor(x)[0]['label'].lower())

    # Select final columns
    result_df = merged_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import os
    import lester as ld
    from transformers import pipeline
    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    import warnings

    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']

    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def sanitize(text):
        return text.lower()

    # Load customer data
    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=',', parse_dates=False)

    # Filter customers based on target countries
    customers_df = customers_df.filter(f'country in {target_countries}')

    # Add 'is_premium' column
    customers_df = customers_df.project('is_premium', ['level'], lambda level: level == 'premium')

    # Select necessary columns
    customers_df = customers_df[['customer_email', 'bank', 'country', 'is_premium']]

    # Load mail data
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=',', parse_dates=False)

    # Extract mail year
    mails_df = mails_df.project('mail_year', ['raw_date'], lambda raw_date: int(raw_date.split("-")[0]))

    # Filter mails from 2022 and onwards
    mails_df = mails_df.filter('mail_year >= 2022')

    # Join customer and mail data on email
    merged_df = ld.join(mails_df, customers_df, left_on='email', right_on='customer_email')

    # Project additional columns
    merged_df = merged_df.project('title', ['mail_subject'], sanitize)
    merged_df = merged_df.project('text', ['mail_text'], sanitize)
    merged_df = merged_df.project('sentiment', ['mail_text'], lambda text: sentiment_predictor(text)[0]['label'].lower())

    # Select final columns
    result_df = merged_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import os
    from transformers import pipeline
    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    import warnings
    import lester as ld

    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']

    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def sanitize(text):
        return text.lower()

    # Read customer data
    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=',')
    
    # Filter target countries
    filtered_customers_df = customers_df.filter('country in @target_countries')
    
    # Determine is_premium
    filtered_customers_df = filtered_customers_df.project(target_column='is_premium', source_columns=['level'], func=lambda level: level == 'premium')
    
    # Read mail data
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=',')
    
    # Extract mail year
    mails_df = mails_df.project(target_column='mail_year', source_columns=['raw_date'], func=lambda raw_date: int(raw_date.split("-")[0]))
    
    # Filter mails from 2022 onwards
    filtered_mails_df = mails_df.filter('mail_year >= 2022')
    
    # Join customers and mails data
    joined_df = ld.join(filtered_mails_df, filtered_customers_df, left_on='email', right_on='customer_email')
    
    # Sanitize mail subject
    joined_df = joined_df.project(target_column='title', source_columns=['mail_subject'], func=sanitize)
    
    # Sanitize mail text
    joined_df = joined_df.project(target_column='text', source_columns=['mail_text'], func=sanitize)
    
    # Predict sentiment
    joined_df = joined_df.project(target_column='sentiment', source_columns=['mail_text'], func=lambda text: sentiment_predictor(text)[0]['label'].lower())
    
    # Select required columns
    result_df = joined_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]
    
    return result_df
