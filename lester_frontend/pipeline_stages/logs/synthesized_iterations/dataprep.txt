
 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import os
    from transformers import pipeline
    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    import warnings
    import lester as ld

    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']
    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def matches_usecase(text):
        return "complaint" in text or "bank account" in text

    def sanitize(text):
        return text.lower()

    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=',', parse_dates=False)
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=',', parse_dates=False)

    customers_df = customers_df.filter("country in @target_countries")
    customers_df = customers_df.project('is_premium', ['level'], lambda x: x == 'premium')
    customers_df = customers_df[['customer_email', 'bank', 'country', 'is_premium']]

    mails_df = mails_df.project('mail_year', ['raw_date'], lambda x: int(x.split("-")[0]))
    mails_df = mails_df.filter("mail_year >= 2022")

    merged_df = ld.join(mails_df, customers_df, left_on='email', right_on='customer_email')

    merged_df = merged_df.project('title', ['mail_subject'], sanitize)
    merged_df = merged_df.project('text', ['mail_text'], sanitize)
    merged_df = merged_df.project('sentiment', ['mail_text'], lambda x: sentiment_predictor(x)[0]['label'].lower())

    result_df = merged_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __featurise():
    import numpy as np
    from sklearn.preprocessing import StandardScaler, OneHotEncoder
    from sklearn.pipeline import Pipeline
    from sklearn.compose import ColumnTransformer
    from sentence_transformers import SentenceTransformer
    from sklearn.base import BaseEstimator, TransformerMixin
    import pandas as pd
    
    # Custom transformer for sentence embeddings
    class SentenceEmbeddingTransformer(BaseEstimator, TransformerMixin):
        def __init__(self, model_name="all-mpnet-base-v2"):
            self.model_name = model_name
            self.embedder = None
            
        def fit(self, X, y=None):
            self.embedder = SentenceTransformer(self.model_name)
            return self
        
        def transform(self, X):
            return self.embedder.encode(X)

    # Custom transformer for word count
    class WordCountTransformer(BaseEstimator, TransformerMixin):
        def fit(self, X, y=None):
            return self
        
        def transform(self, X):
            return np.array([len(text.split(" ")) for text in X]).reshape(-1, 1)

    # Define the transformers
    title_embedding_transformer = Pipeline(steps=[
        ('sentence_embedding', SentenceEmbeddingTransformer())
    ])
    
    text_embedding_transformer = Pipeline(steps=[
        ('sentence_embedding', SentenceEmbeddingTransformer())
    ])
    
    title_length_standardizer = Pipeline(steps=[
        ('word_count', WordCountTransformer()),
        ('scaler', StandardScaler())
    ])
    
    country_onehot_encoder = OneHotEncoder(categories=[['DE', 'FR', 'UK']], drop=None, sparse_output=False)
    
    # Column Transformer
    preprocessor = ColumnTransformer(
        transformers=[
            ('title_embedding', title_embedding_transformer, 'title'),
            ('text_embedding', text_embedding_transformer, 'text'),
            ('title_length', title_length_standardizer, 'title'),
            ('country_onehot', country_onehot_encoder, ['country'])
        ]
    )
    
    return preprocessor

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import os
    from transformers import pipeline
    import warnings
    import lester as ld

    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    warnings.simplefilter(action='ignore', category=FutureWarning)

    # Load data
    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=",", parse_dates=False)
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=",", parse_dates=False)

    target_countries = ['UK', 'DE', 'FR']

    # Initialize sentiment analysis pipeline
    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    # Define helper functions
    def sanitize(text):
        return text.lower()

    def extract_year(raw_date):
        return int(raw_date.split("-")[0])

    def determine_is_premium(level):
        return level == 'premium'

    def get_sentiment(text):
        return sentiment_predictor(text)[0]['label'].lower()

    # Filter customers based on target countries and prepare premium column
    customers_df = customers_df.filter('country in @target_countries')
    customers_df = customers_df.project('is_premium', ['level'], lambda level: determine_is_premium(level))

    # Filter mails based on year
    mails_df = mails_df.project('mail_year', ['raw_date'], extract_year)
    mails_df = mails_df.filter('mail_year >= 2022')

    # Join customers and mails dataframes on email
    joined_df = ld.join(mails_df, customers_df, left_on='email', right_on='customer_email')

    # Create the final columns
    joined_df = joined_df.project('title', ['mail_subject'], sanitize)
    joined_df = joined_df.project('text', ['mail_text'], sanitize)
    joined_df = joined_df.project('sentiment', ['mail_text'], get_sentiment)

    # Select result columns
    result_df = joined_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import os
    from transformers import pipeline
    import warnings
    import lester as ld

    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    warnings.simplefilter(action='ignore', category=FutureWarning)

    # Load data
    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=",", parse_dates=False)
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=",", parse_dates=False)

    target_countries = ['UK', 'DE', 'FR']

    # Initialize sentiment analysis pipeline
    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    # Define helper functions
    def sanitize(text):
        return text.lower()

    def extract_year(raw_date):
        return int(raw_date.split("-")[0])

    def determine_is_premium(level):
        return level == 'premium'

    def get_sentiment(text):
        return sentiment_predictor(text)[0]['label'].lower()

    # Filter customers based on target countries and prepare premium column
    customers_df = customers_df[customers_df['country'].isin(target_countries)]
    customers_df['is_premium'] = customers_df['level'].apply(determine_is_premium)

    # Filter mails based on year
    mails_df['mail_year'] = mails_df['raw_date'].apply(extract_year)
    mails_df = mails_df[mails_df['mail_year'] >= 2022]

    # Join customers and mails dataframes on email
    joined_df = mails_df.merge(customers_df, left_on='email', right_on='customer_email', how='inner')

    # Create the final columns
    joined_df['title'] = joined_df['mail_subject'].apply(sanitize)
    joined_df['text'] = joined_df['mail_text'].apply(sanitize)
    joined_df['sentiment'] = joined_df['mail_text'].apply(get_sentiment)

    # Select result columns
    result_df = joined_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import os
    from transformers import pipeline
    import warnings
    import lester as ld

    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']
    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def matches_usecase(text):
        return "complaint" in text or "bank account" in text

    def sanitize(text):
        return text.lower()

    # Read customer data
    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=',', parse_dates=False)
    
    # Filter for target countries
    customers_df = customers_df.filter('country in @target_countries')
    
    # Create is_premium column
    customers_df = customers_df.project('is_premium', ['level'], lambda x: x[0] == 'premium')
    
    # Read mail data
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=',', parse_dates=False)

    # Filter mails from 2022 onwards
    mails_df = mails_df.filter('int(raw_date.split("-")[0]) >= 2022')

    # Join customers and mails on email
    combined_df = ld.join(mails_df, customers_df, left_on='email', right_on='customer_email')

    # Sanitize mail_subject to title
    combined_df = combined_df.project('title', ['mail_subject'], lambda x: sanitize(x[0]))

    # Sanitize mail_text to text
    combined_df = combined_df.project('text', ['mail_text'], lambda x: sanitize(x[0]))

    # Predict sentiment
    combined_df = combined_df.project('sentiment', ['mail_text'], lambda x: sentiment_predictor(x[0])[0]['label'].lower())

    # Select final columns
    result_df = combined_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import os
    from transformers import pipeline
    import warnings
    import lester as ld

    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']
    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def matches_usecase(text):
        return "complaint" in text or "bank account" in text

    def sanitize(text):
        return text.lower()

    # Read customer data
    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=',', parse_dates=False)

    # Filter customers based on target countries
    customers_df = customers_df.filter('country in @target_countries')

    # Add is_premium column
    def determine_premium(level):
        return level == 'premium'
    
    customers_df = customers_df.project('is_premium', ['level'], determine_premium)
    
    # Read mails data
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=',', parse_dates=False)
    
    # Filter mails based on year
    def filter_year(raw_date):
        mail_year = int(raw_date.split("-")[0])
        return mail_year >= 2022
    
    mails_df = mails_df.filter('filter_year(raw_date)')

    # Join customers and mails dataframes
    joined_df = ld.join(mails_df, customers_df[['customer_email', 'bank', 'country', 'is_premium']], left_on='email', right_on='customer_email')

    # Sanitize mail_subject and mail_text, and determine sentiment
    joined_df = joined_df.project('title', ['mail_subject'], sanitize)
    joined_df = joined_df.project('text', ['mail_text'], sanitize)
    
    def analyze_sentiment(mail_text):
        return sentiment_predictor(mail_text)[0]['label'].lower()
    
    joined_df = joined_df.project('sentiment', ['mail_text'], analyze_sentiment)

    # Select the required columns
    result_df = joined_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import os
    from transformers import pipeline
    import warnings
    import lester as ld

    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']
    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def matches_usecase(text):
        return "complaint" in text or "bank account" in text

    def sanitize(text):
        return text.lower()

    # Load customer data
    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=",", parse_dates=False)
    customers_df = customers_df.filter("country in @target_countries")
    customers_df = customers_df.project('is_premium', ['level'], lambda level: level == 'premium')
    customers_df = customers_df[['customer_email', 'bank', 'country', 'is_premium']]

    # Load mails data
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=",", parse_dates=False)
    mails_df = mails_df.project('mail_year', ['raw_date'], lambda raw_date: int(raw_date.split("-")[0]))
    mails_df = mails_df.filter("mail_year >= 2022")
    
    # Join dataframes
    merged_df = ld.join(mails_df, customers_df, left_on='email', right_on='customer_email')

    # Process data
    merged_df = merged_df.project('title', ['mail_subject'], lambda mail_subject: sanitize(mail_subject))
    merged_df = merged_df.project('text', ['mail_text'], lambda mail_text: sanitize(mail_text))
    merged_df = merged_df.project('sentiment', ['mail_text'], lambda mail_text: sentiment_predictor(mail_text)[0]['label'].lower())
    
    # Select desired columns
    result_df = merged_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import lester as ld
    import os
    from transformers import pipeline
    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    import warnings

    warnings.simplefilter(action='ignore', category=FutureWarning)

    # Helper Functions
    def matches_usecase(text):
        return "complaint" in text or "bank account" in text

    def sanitize(text):
        return text.lower()

    # Constants and setup
    target_countries = ['UK', 'DE', 'FR']
    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    # Load data
    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=',', parse_dates=False)
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=',', parse_dates=False)

    # Filter and prepare customer data
    filtered_customers_df = customers_df.filter('country in @target_countries')
    filtered_customers_df = filtered_customers_df.project('is_premium', ['level'], lambda level: level == 'premium')

    # Merge mails with customer data
    merged_df = ld.join(mails_df, filtered_customers_df, left_on='email', right_on='customer_email')

    # Filter mails by year
    filtered_mails_df = merged_df.project('mail_year', ['raw_date'], lambda raw_date: int(raw_date.split('-')[0]))
    filtered_mails_df = filtered_mails_df.filter('mail_year >= 2022')

    # Create final output columns
    result_df = filtered_mails_df.project('title', ['mail_subject'], sanitize)
    result_df = result_df.project('text', ['mail_text'], sanitize)
    result_df = result_df.project('sentiment', ['mail_text'], lambda mail_text: sentiment_predictor(mail_text)[0]['label'].lower())

    # Select final columns
    result_df = result_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import os
    from transformers import pipeline
    import warnings
    import lester as ld

    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']

    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def matches_usecase(text):
        return "complaint" in text or "bank account" in text

    def sanitize(text):
        return text.lower()

    # Load customer data
    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=',', parse_dates=False)

    # Filter customers from target countries
    customers_df = customers_df.filter('country in @target_countries')

    # Create the is_premium column
    customers_df = customers_df.project(target_column='is_premium', source_columns=['level'], func=lambda level: level == 'premium')

    # Load mail data
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=',', parse_dates=False)

    # Filter mails from 2022 onwards
    mails_df = mails_df.project(target_column='mail_year', source_columns=['raw_date'], func=lambda raw_date: int(raw_date.split("-")[0]))
    mails_df = mails_df.filter('mail_year >= 2022')

    # Join customer and mail data
    joined_df = ld.join(mails_df, customers_df, left_on='email', right_on='customer_email')

    # Sanitize mail_subject and mail_text
    joined_df = joined_df.project(target_column='title', source_columns=['mail_subject'], func=sanitize)
    joined_df = joined_df.project(target_column='text', source_columns=['mail_text'], func=sanitize)

    # Analyze sentiment
    joined_df = joined_df.project(target_column='sentiment', source_columns=['mail_text'], func=lambda text: sentiment_predictor(text)[0]['label'].lower())

    # Select necessary columns
    result_df = joined_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df

 ----------------------------------------------- 

dataprep:


def __dataprep(customers_file, mails_file):
    import os
    from transformers import pipeline
    import warnings
    import lester as ld

    os.environ["TOKENIZERS_PARALLELISM"] = "False"
    warnings.simplefilter(action='ignore', category=FutureWarning)

    target_countries = ['UK', 'DE', 'FR']
    
    sentiment_predictor = pipeline('sentiment-analysis', model='distilbert-base-uncased-finetuned-sst-2-english')

    def matches_usecase(text):
        return "complaint" in text or "bank account" in text

    def sanitize(text):
        return text.lower()

    customers_df = ld.read_csv(customers_file, header=None, names=['customer_id', 'customer_email', 'bank', 'country', 'level'], sep=",", parse_dates=False)
    mails_df = ld.read_csv(mails_file, header=None, names=['mail_id', 'email', 'raw_date', 'mail_subject', 'mail_text'], sep=",", parse_dates=False)
    
    customers_df = customers_df.filter("country in @target_countries")
    customers_df = customers_df.project('is_premium', ['level'], lambda x: x == 'premium')
    customers_df = customers_df[['customer_email', 'bank', 'country', 'is_premium']]

    mails_df = mails_df.project('mail_year', ['raw_date'], lambda raw_date: int(raw_date.split("-")[0]))
    mails_df = mails_df.filter("mail_year >= 2022")

    merged_df = ld.join(mails_df, customers_df, left_on='email', right_on='customer_email')

    merged_df = merged_df.project('title', ['mail_subject'], sanitize)
    merged_df = merged_df.project('text', ['mail_text'], sanitize)
    merged_df = merged_df.project('sentiment', ['mail_text'], lambda text: sentiment_predictor(text)[0]['label'].lower())

    result_df = merged_df[['title', 'text', 'bank', 'country', 'sentiment', 'is_premium']]

    return result_df
